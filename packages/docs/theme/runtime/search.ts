/**
 * Search initialization stub.
 *
 * Pagefind Component UI (<pagefind-modal>, <pagefind-modal-trigger>)
 * handles initialization, keyboard shortcuts (Cmd/Ctrl+K), and
 * focus management natively via web components. No manual JS
 * orchestration is required.
 */

export function initSearch(): void {}
