export { Fragment, h, HtmlString } from '@pagesmith/core/jsx-runtime'

// JSX global declarations moved to a separate .d.ts to avoid
// Element name collision with hast's Element during DTS generation.
