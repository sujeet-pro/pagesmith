%%{init: {'htmlLabels': false}}%%
flowchart TB
  F["Foundations reset and tokens"]
  C["Content tier prose code tabs inline viewport"]
  S["Standalone tier TOC grid sidebar"]
  FN["Fonts export optional separate import"]
  F --> C
  C --> S
  FN -.-> F
